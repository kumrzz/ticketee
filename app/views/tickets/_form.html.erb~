<%= simple_form_for([project, ticket]) do |f| %>
 <%= f.input :name %>
 <%= f.input :description %>
	<%= f.input :attachment, as: :file, label: "File #1" %>
 <%= f.input :attachment_cache, as: :hidden %>

 <%= f.button :submit, class: "btn-primary" %>
<% end %>
